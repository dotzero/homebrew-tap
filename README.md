# dotzero Homebrew Tap

Homebrew tap for dotzero tools and apps.

### git-profile

CLI tool for switching between multiple Git user profiles.

```sh
brew install dotzero/tap/git-profile
```

### JoinNow

Native macOS app for upcoming meeting notifications.

```sh
brew install --cask dotzero/tap/join-now
```
